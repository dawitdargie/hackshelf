# HackShelf — Frontend Environment Configuration
# Copy this file to .env.local and fill in your values.
# Never commit the actual .env.local file to Git.

# --- API ---
# Public URL used by the browser. Server-side rendering can reach the API over
# an internal address instead by setting API_URL_INTERNAL (server-only).
NEXT_PUBLIC_API_URL=http://localhost:8080